# Copy to .env.local and fill in your GitHub token
# Create a token at https://github.com/settings/tokens
# Required for 5000/hr rate limit (vs 60/hr unauthenticated)
# Use public_repo scope for read-only access to public repos

GITHUB_TOKEN=ghp_xxx
